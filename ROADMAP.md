# ROADMAP

## Version 0.5 (Current Release Candidate)

### COMPLETED

- Initial release; POC of graph + enumeration scripts

## Version 1.0 Plan (Release June 1, 2026)

### COMPLETED

### TODO

### Modules

- Look into multi-threading and ways to speed up enumeration (maybe lower retry amount?)
- Make enumeration modules more robust with partial permissions
- Exploit modules are in progress for staged V1 reintegration.
- Manually verify configuration findings from module generated by LLM for enum_config_checks
- Example exploit modules in-progress/to be added in near future:
  - `exploit_create_compute_instance`
  - `exploit_add_user_to_group`
  - `exploit_create_user_api_key`

### Auth Improvements
- Review delegation tokens and see if support is needed

### Engine and policy coverage

- Expand policy parser support for additional statement forms and edge cases.
- Strengthen conditional-scoping and inheritance modeling for mixed tenancy/compartment attachments.
- Continue reducing false negatives for permission-only statement paths.
- Add multi-statement edges for those that are more than 1 permission
- Add cumulative permission traking based off what modules executed successfully (ex. ListUsers worked so current user has INSPECT_USERS permissions)

### Graph quality and explainability

- Add richer edge provenance metadata and improved suppression/explanation for overlapping edges.

### Operator UX and output ergonomics

- Improve downloaded artifact naming/path ergonomics for easier bulk parsing.
- Add additional output profile presets for CI, red-team, and reporting workflows.
- Expand runbook/troubleshooting content for common module and tenancy misconfiguration issues.
- Add tracked permissions UX/reporting to replace the temporary telemetry command surface.
- Add multi-region support with explicit selection flags (for example include/exclude region flags) instead of a single default-region setting.
- Refactor `SessionUtility` into focused internal services (config-state, output-path, credential-loading) while preserving CLI behavior and simplifying maintainability.
